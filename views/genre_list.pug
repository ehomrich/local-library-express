extends layout

block content
    h1= title
  
    ul
        each genre in genresList
            li
                a(href=genre.url) #{genre.name}
        
        else
            li There are no books.